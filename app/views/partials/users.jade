h1 Users
.row
  .col-sm-6.col-md-3(ng-repeat="user in users")
    .thumbnail
      a(href="users/{{user._id}}")
        img.img-responsive(ng-src="{{user.photo}}")
      .caption
        h3 {{user.name}}
        p.text-right
          a.btn.btn-default(href="users/{{user._id}}") More...